{
  "name": "ask-hanseung-kim",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "typegen": "pocketbase-typegen --json ./pb_schema.json --out ./src/lib/pocketbase-types.ts"
  },
  "dependencies": {
    "pocketbase": "^0.28.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.9",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "pocketbase-typegen": "^1.5.0",
    "svelte": "^5.38.10",
    "svelte-check": "^4.3.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "wrangler": "^4.38.0"
  }
}
